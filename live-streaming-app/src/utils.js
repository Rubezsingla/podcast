// src/utils.js

export const handleSuccess = (message) => {
    // Logic for handling success messages, could be a toast or a notification
    console.log('Success:', message);
};

export const handleError = (message) => {
    // Logic for handling error messages, could be a toast or a notification
    console.error('Error:', message);
};
